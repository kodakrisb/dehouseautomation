/**
 * DeHouse launch configuration.
 *
 * REQUIRED BEFORE PUBLIC LAUNCH:
 * Replace the empty WHATSAPP_NUMBER value below with the real DeHouse
 * business WhatsApp number in international format, digits only.
 * Example format: 234XXXXXXXXXX (no +, spaces or dashes).
 *
 * This is the single WhatsApp configuration point used across the site.
 */
window.DEHOUSE_CONFIG = Object.freeze({
  // >>> ADD REAL DEHOUSE WHATSAPP NUMBER HERE <<<
  WHATSAPP_NUMBER: "",

  WHATSAPP_DEFAULT_MESSAGE:
    "Hello DeHouse Automation, I would like to know more about your smart-home devices and installation services.",

  // Optional launch details. Leave blank until the real values are confirmed.
  BUSINESS_PHONE_DISPLAY: "",
  BUSINESS_EMAIL: "",
  INSTAGRAM_URL: "",
  TIKTOK_URL: "",
  FACEBOOK_URL: ""
});
